const express=require('express'),http=require('http'),{Server}=require('socket.io'),session=require('express-session'),bcrypt=require('bcryptjs'),{v4:uuidv4}=require('uuid'),path=require('path'),fs=require('fs'),Database=require('better-sqlite3');
const app=express(),server=http.createServer(app),io=new Server(server,{cors:{origin:'*'}}),PORT=process.env.PORT||8080;
const db=new Database(path.join(__dirname,'proctor.db'));
db.exec(`CREATE TABLE IF NOT EXISTS users(id TEXT PRIMARY KEY,email TEXT UNIQUE NOT NULL,password TEXT NOT NULL,name TEXT NOT NULL,org TEXT NOT NULL,role TEXT DEFAULT 'interviewer',created_at INTEGER DEFAULT(strftime('%s','now')));CREATE TABLE IF NOT EXISTS sessions(id TEXT PRIMARY KEY,code TEXT UNIQUE NOT NULL,title TEXT NOT NULL,candidate_name TEXT,interviewer_id TEXT NOT NULL,team_id TEXT,status TEXT DEFAULT 'waiting',trust_score INTEGER DEFAULT 100,flags TEXT DEFAULT '[]',questions TEXT DEFAULT '[]',display_count INTEGER DEFAULT 1,recording_url TEXT,started_at INTEGER,ended_at INTEGER,created_at INTEGER DEFAULT(strftime('%s','now')));CREATE TABLE IF NOT EXISTS flags(id TEXT PRIMARY KEY,session_id TEXT NOT NULL,time_offset TEXT NOT NULL,text TEXT NOT NULL,detail TEXT,severity TEXT NOT NULL,created_at INTEGER DEFAULT(strftime('%s','now')));CREATE TABLE IF NOT EXISTS teams(id TEXT PRIMARY KEY,name TEXT NOT NULL,org TEXT NOT NULL,owner_id TEXT NOT NULL,invite_code TEXT UNIQUE NOT NULL,created_at INTEGER DEFAULT(strftime('%s','now')));CREATE TABLE IF NOT EXISTS team_members(team_id TEXT NOT NULL,user_id TEXT NOT NULL,role TEXT DEFAULT 'member',PRIMARY KEY(team_id,user_id));`);
app.use(express.json(),express.urlencoded({extended:true}),express.static(path.join(__dirname,'public')));
app.use(session({secret:'proctor-'+Math.random().toString(36),resave:false,saveUninitialized:false,cookie:{maxAge:7*24*60*60*1000}}));
const auth=(req,res,next)=>{if(!req.session.userId)return res.status(401).json({error:'Not authenticated'});next();};
const code6=()=>{const c='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';let r='';for(let i=0;i<6;i++){if(i===3)r+='-';r+=c[Math.floor(Math.random()*c.length)];}return r;};
app.post('/api/auth/signup',async(req,res)=>{const{email,password,name,org}=req.body;if(!email||!password||!name||!org)return res.json({error:'All fields required'});if(password.length<6)return res.json({error:'Password too short'});try{const h=await bcrypt.hash(password,10),id=uuidv4();db.prepare('INSERT INTO users(id,email,password,name,org)VALUES(?,?,?,?,?)').run(id,email.toLowerCase(),h,name,org);req.session.userId=id;req.session.userName=name;req.session.userOrg=org;res.json({ok:true,name,org});}catch(e){res.json({error:e.message.includes('UNIQUE')?'Email already registered':'Signup failed'});}});
app.post('/api/auth/login',async(req,res)=>{const{email,password}=req.body;const u=db.prepare('SELECT* FROM users WHERE email=?').get(email.toLowerCase());if(!u)return res.json({error:'No account found'});if(!await bcrypt.compare(password,u.password))return res.json({error:'Incorrect password'});req.session.userId=u.id;req.session.userName=u.name;req.session.userOrg=u.org;res.json({ok:true,name:u.name,org:u.org});});
app.post('/api/auth/logout',(req,res)=>{req.session.destroy();res.json({ok:true});});
app.get('/api/auth/me',(req,res)=>{if(!req.session.userId)return res.json({loggedIn:false});const u=db.prepare('SELECT id,email,name,org,role FROM users WHERE id=?').get(req.session.userId);res.json({loggedIn:true,...u});});
app.post('/api/sessions',auth,(req,res)=>{const{title,candidateName,teamId,questions}=req.body;const id=uuidv4();let code;do{code=code6();}while(db.prepare('SELECT id FROM sessions WHERE code=?').get(code));db.prepare('INSERT INTO sessions(id,code,title,candidate_name,interviewer_id,team_id,questions,started_at)VALUES(?,?,?,?,?,?,?,?)').run(id,code,title||'Interview',candidateName||'Candidate',req.session.userId,teamId||null,JSON.stringify(questions||[]),Math.floor(Date.now()/1000));res.json({ok:true,id,code});});
app.get('/api/sessions',auth,(req,res)=>{const s=db.prepare('SELECT s.*,u.name as interviewer_name FROM sessions s JOIN users u ON s.interviewer_id=u.id WHERE s.interviewer_id=? OR s.team_id IN(SELECT team_id FROM team_members WHERE user_id=?) ORDER BY s.created_at DESC LIMIT 50').all(req.session.userId,req.session.userId);res.json(s.map(s=>({...s,flags:JSON.parse(s.flags||'[]'),questions:JSON.parse(s.questions||'[]')})));});
app.get('/api/sessions/:id',auth,(req,res)=>{const s=db.prepare('SELECT* FROM sessions WHERE id=?').get(req.params.id);if(!s)return res.json({error:'Not found'});res.json({...s,flags:db.prepare('SELECT* FROM flags WHERE session_id=? ORDER BY created_at').all(req.params.id),questions:JSON.parse(s.questions||'[]')});});
app.get('/api/join/:code',(req,res)=>{const s=db.prepare('SELECT id,code,title,candidate_name,status FROM sessions WHERE code=?').get(req.params.code.toUpperCase());if(!s)return res.json({error:'Not found'});if(s.status==='ended')return res.json({error:'Session ended'});res.json({ok:true,...s});});
app.patch('/api/sessions/:id',auth,(req,res)=>{const{status,trustScore,displayCount,candidateName}=req.body;const s=db.prepare('SELECT id FROM sessions WHERE id=? AND interviewer_id=?').get(req.params.id,req.session.userId);if(!s)return res.json({error:'Not found'});if(status)db.prepare('UPDATE sessions SET status=? WHERE id=?').run(status,req.params.id);if(trustScore!==undefined)db.prepare('UPDATE sessions SET trust_score=? WHERE id=?').run(trustScore,req.params.id);if(displayCount!==undefined)db.prepare('UPDATE sessions SET display_count=? WHERE id=?').run(displayCount,req.params.id);if(candidateName)db.prepare('UPDATE sessions SET candidate_name=? WHERE id=?').run(candidateName,req.params.id);if(status==='ended')db.prepare('UPDATE sessions SET ended_at=? WHERE id=?').run(Math.floor(Date.now()/1000),req.params.id);res.json({ok:true});});
app.post('/api/sessions/:id/flags',(req,res)=>{const{text,detail,severity,timeOffset}=req.body;db.prepare('INSERT INTO flags(id,session_id,time_offset,text,detail,severity)VALUES(?,?,?,?,?,?)').run(uuidv4(),req.params.id,timeOffset||'00:00',text,detail||'',severity||'medium');res.json({ok:true});});
app.post('/api/teams',auth,(req,res)=>{const{name}=req.body;if(!name)return res.json({error:'Name required'});const u=db.prepare('SELECT org FROM users WHERE id=?').get(req.session.userId);const id=uuidv4();let code;do{code=uuidv4().slice(0,8).toUpperCase();}while(db.prepare('SELECT id FROM teams WHERE invite_code=?').get(code));db.prepare('INSERT INTO teams(id,name,org,owner_id,invite_code)VALUES(?,?,?,?,?)').run(id,name,u.org,req.session.userId,code);db.prepare('INSERT INTO team_members(team_id,user_id,role)VALUES(?,?,?)').run(id,req.session.userId,'owner');res.json({ok:true,id,inviteCode:code});});
app.get('/api/teams',auth,(req,res)=>{const t=db.prepare('SELECT t.* FROM teams t JOIN team_members tm ON t.id=tm.team_id WHERE tm.user_id=?').all(req.session.userId);res.json(t.map(t=>({...t,members:db.prepare('SELECT u.id,u.name,u.email,tm.role FROM team_members tm JOIN users u ON tm.user_id=u.id WHERE tm.team_id=?').all(t.id)})));});
app.post('/api/teams/join',auth,(req,res)=>{const{inviteCode}=req.body;const t=db.prepare('SELECT* FROM teams WHERE invite_code=?').get(inviteCode);if(!t)return res.json({error:'Invalid code'});if(db.prepare('SELECT 1 FROM team_members WHERE team_id=? AND user_id=?').get(t.id,req.session.userId))return res.json({error:'Already a member'});db.prepare('INSERT INTO team_members(team_id,user_id)VALUES(?,?)').run(t.id,req.session.userId);res.json({ok:true,teamName:t.name});});
const servePage=(file)=>(req,res)=>{const p=require('path').join('/opt/render/project/src/public/pages',file);console.log('Serving:',p);require('fs').access(p,err=>{if(err){console.error('File missing:',p,err.message);return res.status(404).send('File not found: '+p);}res.sendFile(p,err=>{if(err){console.error('sendFile error:',err.message);if(!res.headersSent)res.status(500).send('sendFile failed: '+err.message);}});});};app.get('/',servePage('index.html'));
app.get('/dashboard',servePage('dashboard.html'));
app.get('/session/:id',servePage('session.html'));
app.get('/join/:code',servePage('candidate.html'));
const rooms={};
io.on('connection',socket=>{
  socket.on('join-room',({sessionId,role})=>{socket.join(sessionId);if(!rooms[sessionId])rooms[sessionId]={};rooms[sessionId][role]=socket.id;socket.data.sessionId=sessionId;socket.data.role=role;socket.to(sessionId).emit('peer-joined',{role});const o=Object.entries(rooms[sessionId]).filter(([r])=>r!==role);if(o.length)socket.emit('peer-already-present',{role:o[0][0]});});
  socket.on('webrtc-offer',d=>socket.to(d.sessionId).emit('webrtc-offer',{offer:d.offer}));
  socket.on('webrtc-answer',d=>socket.to(d.sessionId).emit('webrtc-answer',{answer:d.answer}));
  socket.on('webrtc-ice',({sessionId,candidate})=>socket.to(sessionId).emit('webrtc-ice',{candidate}));
  socket.on('recording-started',({sessionId})=>socket.to(sessionId).emit('recording-started'));
  socket.on('recording-stopped',({sessionId})=>socket.to(sessionId).emit('recording-stopped'));
  socket.on('next-question',({sessionId,qIdx})=>socket.to(sessionId).emit('next-question',{qIdx}));
  socket.on('candidate-flag',({sessionId,...flag})=>{socket.to(sessionId).emit('candidate-flag',flag);try{db.prepare('INSERT INTO flags(id,session_id,time_offset,text,detail,severity)VALUES(?,?,?,?,?,?)').run(uuidv4(),sessionId,flag.time||'00:00',flag.text,flag.detail||'',flag.sev||'medium');const p={high:12,medium:6,low:2}[flag.sev]||5;db.prepare('UPDATE sessions SET trust_score=MAX(0,trust_score-?) WHERE id=?').run(p,sessionId);}catch(e){}});
  socket.on('disconnect',()=>{const{sessionId,role}=socket.data;if(sessionId&&rooms[sessionId]){delete rooms[sessionId][role];socket.to(sessionId).emit('peer-left',{role});}});
});
server.listen(PORT,'0.0.0.0',()=>{console.log('Secure Interview running on port '+PORT);setInterval(()=>{require('http').get('http://localhost:'+PORT+'/api/auth/me',r=>{r.resume();}).on('error',()=>{});},14*60*1000);});